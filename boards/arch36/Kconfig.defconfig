if SHIELD_ARCH36_LEFT

config ZMK_KEYBOARD_NAME
	default "Arch 36"

config ZMK_SPLIT_BLE_ROLE_CENTRAL
  default y

endif


if SHIELD_ARCH36_RIGHT

config ZMK_KEYBOARD_NAME
	default "Arch 36 Right"

endif

if SHIELD_ARCH36_LEFT || SHIELD_ARCH36_RIGHT

config ZMK_SPLIT
	default y
 
endif
